#!/usr/bin/env ruby
require './files'

filename = ARGV.first || 'commands.txt'

toy_robot = ToyRobot.new(Robot.new(Coordinate, Direction), Controller.new)
begin
  toy_robot.run filename
rescue Exception => e
  puts e.message
end