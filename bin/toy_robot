#!/usr/bin/env ruby
require './files'

filename = ARGV.first || 'commands.txt'

toy_robot = ToyRobot.new(Robot.new, Controller.new)
begin
  toy_robot.run filename
rescue Exception => e
  puts e.message
end